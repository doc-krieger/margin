# Margin

Local-first document workspace foundation.

## Development

```sh
npm install
npm test
npm run typecheck
npm run build
```

The foundation exposes a typed Fastify health boundary and root-safe path resolver. Document persistence is intentionally not present in the baseline migration.
